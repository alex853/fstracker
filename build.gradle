version '1.0-SNAPSHOT'

apply plugin: 'java'

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url 'https://mymavenrepo.com/repo/9k6aQjU4o2RPUv5zfolv/'
    }
    flatDir {
        dirs 'fsuipc'
    }
}

dependencies {
//    implementation name: 'fsuipc'

    compile group: 'net.simforge', name: 'simforge-commons', version: '1.0.3'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}
